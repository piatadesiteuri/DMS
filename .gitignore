# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables (keep some .env files)
.env.local
.env.development.local
.env.test.local

# Logs (specific log files only)
backend.log
frontend.log
ngrok-demo.log
backend-demo.log
download-server.log
server.log

# Database files (keep SQL files, exclude DB files)
*.db
digital_documents_db
digital_documents_db.db
database2.db

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Build outputs (specific ones)
sync-agent/dist/
sync-agent/build/
front-end/build/

# Temporary files
temp_ocr_test/

# Configuration files with sensitive data
my-remote.cnf
my-temp.cnf

# Large executable and archive files only
*.exe
*.msi
*.dmg
*.pkg
*.deb
*.rpm
*.zip
*.rar
*.7z
*.tar.gz

# Sync agent large builds only
sync-agent/EDMS Sync Agent*.exe
sync-agent/EDMS Sync Agent*.zip
back-end/sync-agent-archive/
back-end/sync-agent-dist/

# Lock files (keep package-lock.json but ignore others)
yarn.lock
pnpm-lock.yaml

# Coverage reports
coverage/
*.lcov

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Electron cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt

# Gatsby files
.cache/

# Storybook build outputs
.out
.storybook-out
